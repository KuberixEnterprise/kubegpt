apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubegpt.fullname" . }}-test-connection"
  labels:
    {{- include "kubegpt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubegpt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
