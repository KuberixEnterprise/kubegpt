apiVersion: v1
kind: Pod
metadata:
  name: warning-pod
spec:
  containers:
    - name: busybox
      image: busybox
      resources:
        limits:
          memory: "1Mi" # 매우 낮은 메모리 제한
          cpu: "100m" # 매우 낮은 CPU 제한
      command: ["sh", "-c", "echo Hello Kubernetes! && sleep 3600"]
